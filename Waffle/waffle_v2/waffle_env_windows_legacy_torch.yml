name: Waffle_Torch
channels:
  - conda-forge
  - defaults
variables:
  KMP_DUPLICATE_LIB_OK: "TRUE"
  OMP_NUM_THREADS: "1"
dependencies:
  - python=3.11
  - numpy
  - pip
  - pip:
      - --index-url https://download.pytorch.org/whl/cpu
      - torch
      - torchvision
      - torchaudio
